pipeline { // Declarative pipelineであることを宣言する
  agent any
  parameters {
        // ticket no
        string(
            name: 'text',
            defaultValue: 'x-japan',
            description: ''
        )
    }
  stages {
    stage('find files') {
        steps {
          copyArtifacts fingerprintArtifacts: true, projectName: 'dev-myRepository'
        }
    }
    stage('push') {
        steps {
          sh """
            git remote add origin https://chiruchiruchiru:5510008chiru@github.com/chiruchiruchiru/release.git
            git checkout master
            git add .
            git config --global user.email 'chiru.5emon@gmail.com'
            git config --global user.name 'chiruchiruchiru'
            git commit -m 'commit'
            git push origin master
          """
        }
    }
  }
}