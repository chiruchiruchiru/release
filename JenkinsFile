pipeline { // Declarative pipelineであることを宣言する
  agent any
  parameters {
        // ticket no
        string(
            name: 'text',
            defaultValue: 'x-japan',
            description: ''
        )
    }
  stages {
    stage("get resource") {
      steps {
        echo "${params.text}"
      }
    }
  }
}